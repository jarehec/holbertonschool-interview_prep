#ifndef SORT_H
#define SORT_H
#include <stdio.h>
#include <stdlib.h>
void print_array(const int *, size_t);
void merge(int *, int *, size_t, size_t, size_t);
void split_merge(int *, int *, size_t, size_t);
void merge_sort(int *, size_t);


#endif
